// SINGLE US SELECTOR LOOKS LIKE:

// const commsVisible = useSelector(
//   state => state.comms.commsVisible
// );

// const selectedCommType = useSelector(
//   state => state.comms.selectedCommType
// )


// TODO: 

// - build specific components with redux baked in,
// - break out globals into another folder (in the same lib for now)
// - write out reqs per the angular file
